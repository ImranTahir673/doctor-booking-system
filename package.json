{
  "name": "doctor-booking-system-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "admin"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix admin"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
